import React from 'react';
import { useLoaderData } from 'react-router-dom';

const SingleCategory = () => {
    const { } = useLoaderData();


    return (
        <div>
            <h2>Helooo category</h2>
        </div>
    );
};

export default SingleCategory;